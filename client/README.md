# MarletMeets Client

## Setup

1. Install dependencies:
   ```bash
   npm install
   ```
2. Copy env template:
   ```bash
   cp .env.example .env
   ```
3. Start dev server:
   ```bash
   npm start
   ```

App runs on `http://localhost:3000` and calls Flask at `http://localhost:5000/api` by default.
